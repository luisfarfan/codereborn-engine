"""
ContextBuilder agent.

Responsibility: Analyse the repository structure and produce a rich
ArchitectureContextPayload — identifying patterns, entry points,
key modules, and a dependency graph.

Outputs: ArchitectureContextPayload → stored in architecture_contexts table.
"""
